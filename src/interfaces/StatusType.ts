export type ReservationStatus = 'pending' | 'confirmed' | 'completed' | 'cancelled';
export type RestaurantStatus = 'active' | 'inactive';

export type StatusType = ReservationStatus | RestaurantStatus;
